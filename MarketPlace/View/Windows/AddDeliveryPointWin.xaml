<Window x:Class="MarketPlace.View.Windows.AddDeliveryPointWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarketPlace.View.Windows"
        mc:Ignorable="d"
        Title="AddDeliveryPointWin" Height="300" Width="300">
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <TextBlock Text="Title"/>
            <TextBox Name="AdressTb"
                     Text="{Binding Adress}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Save" x:Name="SaveBtn"
                        Click="SaveBtn_Click"
                        Width="70"/>
                <Button Content="Exit" x:Name="ExitBtn"
                        Click="ExitBtn_Click"
                        Width="70"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
