<Window x:Class="MarketPlace.View.Windows.AddProviderWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarketPlace.View.Windows"
        mc:Ignorable="d"
        Title="AddProviderWin"  Height="300" Width="300">
    <Grid>
        <StackPanel Orientation="Vertical"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" >
                <TextBlock Text="Title"/>
                <TextBox x:Name="TitleTb" Text="{Binding Title}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Adress"/>
                <TextBox x:Name="AdressTb" Text="{Binding Adress}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Save"
                        x:Name="SaveBtn"
                        Click="SaveBtn_Click"
                        Width="50"
                        />


            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
